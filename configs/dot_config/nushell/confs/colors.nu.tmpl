# Generated by chezmoi from .chezmoidata/colors.toml
# Do not edit — changes will be overwritten
# See docs/colorscheme.md for reference

# Color palette for nushell — sourced by theme.nu
# Uses catppuccin color names for compatibility with theme.nu

let theme = {
    rosewater: "#{{ .colors.palette.coral }}"
    flamingo: "#{{ .colors.palette.salmon }}"
    pink: "#{{ .colors.palette.pink }}"
    mauve: "#{{ .colors.palette.purple }}"
    red: "#{{ .colors.palette.red }}"
    maroon: "#{{ .colors.palette.red_soft }}"
    peach: "#{{ .colors.palette.orange }}"
    yellow: "#{{ .colors.palette.yellow }}"
    green: "#{{ .colors.palette.green }}"
    teal: "#{{ .colors.palette.teal }}"
    sky: "#{{ .colors.palette.cyan }}"
    sapphire: "#{{ .colors.palette.azure }}"
    blue: "#{{ .colors.palette.blue }}"
    lavender: "#{{ .colors.palette.lilac }}"
    text: "#{{ .colors.ui.fg }}"
    subtext1: "#{{ .colors.ui.fg_secondary }}"
    subtext0: "#{{ .colors.ui.fg_muted }}"
    overlay2: "#{{ .colors.ui.fg_faint }}"
    overlay1: "#{{ .colors.ui.border }}"
    overlay0: "#{{ .colors.ui.border_muted }}"
    surface2: "#{{ .colors.ui.surface_raised }}"
    surface1: "#{{ .colors.ui.surface }}"
    surface0: "#{{ .colors.ui.surface_sunken }}"
    base: "#{{ .colors.ui.bg }}"
    mantle: "#{{ .colors.ui.bg_alt }}"
    crust: "#{{ .colors.ui.bg_deep }}"
}
