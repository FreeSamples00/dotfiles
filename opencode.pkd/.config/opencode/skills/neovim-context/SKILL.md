---
name: neovim-context
description: Neovim configuration details including plugin management and language tools
---

## Configuration Overview

Custom neovim config designed to be minimal in both UI and plugin usage.

## Plugin Management

- Uses `lazy.nvim` for plugin management
- Aims for blazing fast boot times via lazy loading

## Language Tools

Custom language tool management where tools are:
- Declared in a config
- Installed manually as needed

## Config Location

`~/dotfiles/neovim.pkd/.config/nvim/`

## Editor Alias

The alias `e` opens neovim.
